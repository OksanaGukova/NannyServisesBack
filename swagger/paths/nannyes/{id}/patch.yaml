patch:
  tags:
    - Nannyes
  summary: Update nanny partially
  description: Updates nanny fields and optionally uploads a photo
  security:
    - bearerAuth: []

  parameters:
    - in: path
      name: nannyId
      required: true
      schema:
        type: string
      description: Nanny ID

  requestBody:
    required: false
    content:
      multipart/form-data:
        schema:
          $ref: '../../components/schemas/nannyUpdate.yaml'

  responses:
    '200':
      description: Successfully patched nanny
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: integer
                example: 200
              message:
                type: string
                example: Successfully patched a nanny!
              data:
                $ref: '../../components/schemas/nanny.yaml'

    '401':
      $ref: '../../components/responses/401.yaml'
    '404':
      $ref: '../../components/responses/404.yaml'
    '500':
      $ref: '../../components/responses/500.yaml'
