tags:
  - Users
summary: Reset password
operationId: resetPassword
description: Reset password using a valid token and new password. The token is issued by the "request-reset-email" flow and must be provided in the request body.
security:
bearerAuth: []
requestBody:
  required: true
  content:
    application/json:
      examples:
        resetExample:
          summary: Example reset request
          value:
            token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
            password: "NewStr0ngP@ss!"
responses:
  '200':
    description: Password has been successfully reset.
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/sessions.yaml'
        examples:
          success:
            value:
              status: 200
              message: "Password has been successfully reset."
              data: {}
  '400':
    $ref: '../../components/responses/400.yaml'
  '401':
    ...
  '429':
    $ref: '../../components/responses/429.yaml'
  '500':
    $ref: '../../components/responses/500.yaml'
